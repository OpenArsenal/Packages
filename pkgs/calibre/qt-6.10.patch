diff --git a/src/calibre/headless/CMakeLists.txt b/src/calibre/headless/CMakeLists.txt
index 1fd7f0d..a685805 100644
--- a/src/calibre/headless/CMakeLists.txt
+++ b/src/calibre/headless/CMakeLists.txt
@@ -2,6 +2,19 @@ cmake_minimum_required(VERSION 3.21)
 project(headless)
 set(CMAKE_AUTOMOC ON)
 __FIND_GUI__
+
+find_package(Qt6Gui REQUIRED)
+
+# Qt private targets (Qt::CorePrivate / Qt::GuiPrivate) are not always provided
+# by the public Qt6Gui package anymore (notably in Qt 6.10+ packaging layouts).
+# calibre's headless platform plugin may link against these private targets via
+# __LINK_TARGETS__, so ensure the packages that define them are discoverable.
+#
+# If the targets are already present, this is a no-op.
+if (NOT TARGET Qt::CorePrivate OR NOT TARGET Qt::GuiPrivate)
+  find_package(Qt6 COMPONENTS CorePrivate GuiPrivate REQUIRED)
+endif()
+
 add_library(headless MODULE main.cpp headless_backingstore.cpp headless_integration.cpp)
 set_property(TARGET headless PROPERTY QT_PLUGIN_TYPE "platforms")
 set_property(TARGET headless PROPERTY QT_PLUGIN_CLASS_NAME "HeadlessIntegrationPlugin")
