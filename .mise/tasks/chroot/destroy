#!/usr/bin/env bash
#MISE description="Destroy the clean package-build chroot"
#MISE hide=true
#MISE confirm={ message = "Delete the configured build chroot?", default = "no" }

source "${PKGBUILDS_ROOT}/.mise/lib/task.sh"
source "${PKGBUILDS_ROOT}/.mise/lib/chroot.sh"

task::bootstrap
chroot::destroy
