#!/usr/bin/env bash
#MISE description="Create the configured repository directory"
#MISE hide=true

source "${PKGBUILDS_ROOT}/.mise/lib/task.sh"

task::bootstrap
task::require_env REPO_DIR

mkdir -p "$REPO_DIR"
