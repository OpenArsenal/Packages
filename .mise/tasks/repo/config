#!/usr/bin/env bash
#MISE description="Print the pacman.conf stanza for this repository"

set -euo pipefail

: "${REPO_NAME:?REPO_NAME not set}"
: "${REPO_DIR:?REPO_DIR not set}"

cat <<EOF
[${REPO_NAME}]
SigLevel = Optional TrustAll
Server = file://${REPO_DIR}
EOF
