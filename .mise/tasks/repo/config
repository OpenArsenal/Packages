#!/usr/bin/env bash
#MISE description="Print the pacman.conf stanza for this repository"

source "${PKGBUILDS_ROOT}/.mise/lib/task.sh"
source "${PKGBUILDS_ROOT}/.mise/lib/repo.sh"

task::bootstrap
task::require_env REPO_NAME REPO_DIR

repo::pacman_stanza "$REPO_NAME" "$REPO_DIR"
