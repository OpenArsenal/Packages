#!/usr/bin/env bash
#MISE description="Initialize the repository-local GPG home"
#MISE hide=true

source "${PKGBUILDS_ROOT}/.mise/lib/task.sh"

task::bootstrap
task::require_env GNUPGHOME

mkdir -p "$GNUPGHOME"
chmod 700 "$GNUPGHOME"
gpg --homedir "$GNUPGHOME" --list-keys >/dev/null
